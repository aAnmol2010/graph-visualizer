## Agent skills

### Issue tracker

Issues and PRDs are tracked via GitHub Issues (external PRs are not a triage surface). See `docs/agents/issue-tracker.md`.

### Triage labels

Using the default triage labels (`needs-triage`, `needs-info`, etc.). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repository layout (`CONTEXT.md` and `docs/adr/` at the root). See `docs/agents/domain.md`.
